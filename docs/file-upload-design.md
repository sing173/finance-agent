# 文件上传方案决策记录

> 来源：grill-with-docs 会话，2026-05-15
> 状态：已对齐，待实现

---

## 核心流程

### 单文件模式

```
选文件 → 后端自动检测银行+格式 → 结果卡片展示"检测到：招商银行 · 账务明细清单"
         → 用户确认解析 → 解析成功 / 失败
```

### 批量模式（B 方案 + 跳过继续）

```
[批量解析] 入口 → 多选文件（最多5个） → 前端过滤格式 → 后端并行检测银行
→ 文件列表（文件名 + 检测状态）
→ fallback 面板（银行 + 表格类型 + OCR）
→ 逐个解析，跳过失败继续
→ 按文件分组折叠展示 + 全部导出
```

---

## 已对齐决策

| # | 问题 | 决策 |
|---|------|------|
| 1 | 检测失败/猜错银行时是否有覆盖路径 | **有**——fallback 面板让用户手动选择 |
| 2 | 文档内容类型（流水/回单）是否展示 | **展示**，不加括号说明，只展示原始文档名 |
| 3 | 单文件 PDF 路由方式 | **A（一步检测）**——检测和解析合并，失败才弹出 fallback |
| 4 | Fallback 面板结构 | 银行 + 表格类型 + OCR（仅 PDF），默认保留已选值 |
| 5 | 表格类型选项来源 | **写死** `['流水', '回单']` |
| 6 | 银行选项来源 | **后端动态获取** `get_supported_banks()` |
| 7 | 批量需求 | **有批量需求** |
| 8 | 批量入口 + 检测方式 | **B（队列累加）+ 混合模式**（能自动检测自动解析，失败才 pop fallback） |
| 9 | 批量流程顺序 | **B（先选文件后配置）** + **跳过继续** |
| 10 | 批量文件列表展示 | **B（文件名 + 检测状态）**，先检测一轮再展示 |
| 11 | 单/批量入口 | **A（独立入口）**，两个卡片 |
| 12 | 批量结果展示 | **B（按文件分组折叠）** |
| 13 | 批量导出操作 | **B（单文件重试 + 全部导出）** |
| 14 | 全部导出范围 | **A（仅成功文件）** |
| 15 | 新文件覆盖旧结果 | **A（直接覆盖）**，互斥状态 |
| 16 | 批量解析进度展示 | **B（进度条 + 当前文件名）** |
| 17 | 解析进度是否阻塞 | **阻塞** |
| 18 | 解析结果持久化 | **B（会话级）**，`currentResult` 和 `batchResult` 互斥 |
| 19 | 错误信息展示 | **A（始终一句话）**，无详细错误开关 |
| 20 | 解析成功后默认行为 | **A（停在结果页）**，保留原有两个导出按钮 |
| 21 | 批量文件类型约束 | **B（选择后校验）**，格式 + 银行类型不一致都提醒 |
| 22 | 批量失败文件重试 | **A（事后补跑）**，复用上次配置 |
| 23 | 检测轮次深度 | **B（扩展名 + 后端银行关键字匹配）** |
| 24 | 批量配置面板默认值 | **A（从第一个成功检测文件推导）** |
| 25 | 表格类型下拉选项 | **A（固定通用值）** 不随银行动态变化 |
| 26 | 单/批量结果互斥 | **A（不保留单文件结果）**，进入批量后单文件清空 |
| 27 | 强制 OCR 选项 | **B（仅 PDF 显示）** |
| 28 | 批量结果 UI 组件 | **A（折叠面板）**，每文件一个折叠项 |
| 29 | 进度/结果展示视图 | **A（同页滚动）** |
| 30 | 批量模式文件数量限制 | **最多 5 个** |
| 31 | 批量文件内交易表格列 | **与单文件保持一致** |
| 32 | 单个文件折叠项内操作 | **C（完整操作按钮）** → 后改为：**无独立导出，保留全部导出** |
| 33 | 单文件视图数据来源 | **A（从 batchResult 读取）** |
| 34 | 批量解析中途取消 | **B（有取消按钮），但取消后不保留结果**，从头开始 |
| 35 | 批量文件数量限制配置 | **B（public/batch_config.json）**，不依赖代码重建 |
| 36 | Fallback 面板组件 | **A（共用 ManualOverrideModal）**，动态文案区分单/批量 |
| 37 | 批量 fallback「解析」按钮行为 | **A（批量解析所有失败文件）**，简化版先不做文件级覆盖 |
| 38 | 批量识别阶段并行方式 | **B（detect_banks 批量 RPC）**，独立方法而非复用 parse_pdf |
| 39 | 已选文件列表增删 | **A（每行删除按钮）**，删除后按钮动态更新 |
| 40 | 全部导出按钮文案 | **动态显示成功数量**，如 `[全部导出 Excel（成功 49）]` |
| 41 | 已导出标记持久化 | **A（仅当前会话）** |
| 42 | 结果卡片文案（手动覆盖后） | **B（"已选择"区分）** |
| 43 | 结果卡片按钮布局 | `[重新检测]` + `[修改配置]` + `[重新选择文件]`，三个按钮 |
| 44 | 修改配置行为 | 打开 fallback 面板预填当前检测值，确认后重新解析 |
| 45 | 折叠面板标题字段 | 文件名 + ✓/✗笔数 + 银行 + 表格类型 + **账单日期**，文件大小不展示 |
| 46 | 失败文件折叠标题 | `▶ filename.pdf  ✗ 解析失败  [重试]`，无银行/类型/日期 |
| 47 | 失败文件收起样式 | **B（标红边框/背景）**，快速定位 |
| 48 | 折叠面板操作 | 加 `[全部展开]` / `[全部收起]` 按钮 |
| 49 | 单文件详情视图功能 | **A（完全一致）**，[重新选择文件] 回到批量结果 |
| 50 | 单文件 fallback 错误卡片按钮 | **A（只有 [修改配置]）**，不额外加重新检测 |

---
