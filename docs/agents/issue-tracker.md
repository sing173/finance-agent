# Issue tracker

Issues live in the GitHub repository at https://github.com/sing173/finance-agent.

Skills `to-issues`, `triage`, `to-prd`, and `qa` use the `gh` CLI to read and create issues. Run `gh issue list`, `gh issue create`, etc. against the `sing173/finance-agent` repo.
